[project]
name = "silicon-trail"
version = "0.1.0"
description = "CLI simulation game"
authors = [{ name = "Linda Eng" }]
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.setuptools.packages.find]
include = ["db*", "game*", "utils*"]